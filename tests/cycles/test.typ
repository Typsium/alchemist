#import "../../lib.typ": *

#skeletize({
  fragment("A")
  cycle(
    5,
    {
      single()
      fragment("B")
      double()
      fragment("C")
      single()
      fragment("D")
      single()
      fragment("E")
      double()
    },
  )
})

#skeletize({
  single()
  fragment("A")
  cycle(
    5,
    align: true,
    {
      single()
      fragment("B")
      double()
      fragment("C")
      single()
      fragment("D")
      single()
      fragment("E")
      double()
    },
  )
})

#skeletize({
  cycle(
    4,
    {
      single()
      fragment("A")
      single()
      fragment("B")
      single()
      fragment("C")
      single()
      fragment("D")
    },
  )
})

#skeletize({
  cycle(
    5,
    {
      branch({
        single()
        fragment("A")
        double()
        fragment("B")
        single()
        fragment("C")
      })
      single()
      branch({
        single()
        fragment("D")
        single()
        fragment("E")
      })
      single()
      branch({
        double()
      })
      single()
      branch({
        single()
        fragment("F")
      })
      single()
      branch({
        single()
        fragment("G")
        double()
      })
      single()
      single()
      single()
      single()
    },
  )
})

#skeletize({
  fragment("A")
  cycle(
    7,
    {
      single()
      fragment("B")
      cycle(
        5,
        {
          single()
          single()
          single()
          single()
        },
      )
      double()
      single()
      double()
      cycle(
        4,
        {
          single()
          single()
          single()
        },
      )
      single()
      double()
      single()
    },
  )
})

#skeletize({
  fragment("AB")
  cycle(
    5,
    {
      single(from: 1, to: 0)
      fragment("CDE")
      single(from: 0)
      fragment("F")
      single(to: 0)
      fragment("GH")
      single(from: 0)
      fragment("I")
      single(to: 1)
    },
  )
})
